/**
 * stringify
 * ----------------------------------------------------------------------------
 * turn a JSON object into a string version
 */
export declare function stringify(obj: any, asHtml: boolean, prefix?: string): string;
